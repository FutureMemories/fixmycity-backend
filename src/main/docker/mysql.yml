version: '2'
services:
    fixmycity-mysql:
        container_name: fixmycity-mysql
        image: mysql:5.7.11
        # volumes:
        #     - ~/volumes/jhipster/fixmycity/mysql/:/var/lib/mysql/
        environment:
            - MYSQL_USER=root
            - MYSQL_ALLOW_EMPTY_PASSWORD=yes
            - MYSQL_DATABASE=fixmycity
        ports:
            - 3306:3306
        command: mysqld --lower_case_table_names=1 --skip-ssl
